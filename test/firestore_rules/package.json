{
  "name": "yno-firestore-rules-tests",
  "private": true,
  "type": "module",
  "description": "Adversarial tests for firestore.rules (invite-code secrecy). Not part of flutter test.",
  "scripts": {
    "test": "npx firebase-tools@13.35.1 emulators:exec --only firestore --project yno-rules-test \"node test/firestore_rules/invite_code_rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0"
  }
}
