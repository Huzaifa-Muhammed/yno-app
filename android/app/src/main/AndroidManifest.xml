<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Contact-based friend discovery (Community → Contacts). -->
    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    <application
        android:label="ynoapp"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Referral links: https://nellab.org/r/<code> opens the app
                 directly instead of the browser.

                 android:autoVerify="true" is what removes the "open with"
                 chooser, and it is NOT free — Android verifies it by fetching
                 https://nellab.org/.well-known/assetlinks.json and checking
                 that this app's signing certificate SHA-256 is listed there.
                 If that file is missing, unreachable, or lists the wrong
                 fingerprint, verification fails silently: no crash, no log the
                 user ever sees, the link just opens in Chrome as though this
                 filter were absent.

                 ⚠️ Two fingerprints are needed once the app is on Play, not
                 one — Play App Signing re-signs the upload with Google's own
                 certificate, so the SHA-256 in Play Console → Setup → App
                 signing must be listed alongside the local release key's.
                 Getting this wrong is the single most likely reason a shipped
                 referral link "does nothing".

                 The web page at /r/<code> stays useful either way: it is what
                 someone without the app sees, and it carries the code on to
                 the Play Store as `referrer=`. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https"
                      android:host="nellab.org"
                      android:pathPrefix="/r/"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
